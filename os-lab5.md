# МІНІСТЕРСТВО ОСВІТИ І НАУКИ УКРАЇНИ  
## КИЇВСЬКИЙ ФАХОВИЙ КОЛЕДЖ ЗВ’ЯЗКУ  

---

# ЗВІТ  
## про виконання лабораторної роботи №5
### з дисципліни «Операційні системи»

---

**Тема:**  
Знайомство з командами навігації по файловій системі та керування файлами та каталогами

---

**Виконала:**  
студентка групи **БІКС-33**  
**Сербіна Ярослава Вячеславівна**

---

**Перевірила:**  
**Сушанова Вікторія Сергіївна**

---

**Київ - 2026**

---

# Лабораторна робота №5
## Операційні системи

---

## Тема
Знайомство з командами навігації по файловій системі та керування файлами та каталогами

---

## Мета роботи
1. Отримання практичних навиків роботи з командною оболонкою Bash.
2. Знайомство з базовими командами навігації по файловій системі.
3. Знайомство з базовими командами для керування файлами та каталогами.


---

## Матеріальне забезпечення занять:
1. ЕОМ типу IBM PC.  
2. ОС сімейства Windows та віртуальна машина VirtualBox (Oracle).  
3. ОС GNU/Linux (будь-який дистрибутив).  
4. Сайт мережевої академії Cisco netacad.com та його онлайн курси по Linux.

---

## Завдання для попередньої підготовки
### Glossary of Basic English Terms 

**Filesystem - a hierarchical structure used by an operating system to organize and store files and directories.**

**Root directory (/) - the top-level directory in Linux from which all other directories originate.**

**Home directory (~) - a personal directory assigned to a user account where personal files and settings are stored.**

**Current working directory - the directory in which the user is currently located in the command-line interface.**

**pwd (print working directory) - a command that displays the full path of the current working directory.**

**cd (change directory) - a command used to navigate between directories in the filesystem.**

**ls (list) - a command used to display the contents of a directory.**

**ls -l - an option of the ls command that displays directory contents in long format, including permissions, owner, size, and modification date.**

**ls -a - an option of the ls command that displays all files, including hidden files.**

**mkdir (make directory) - a command used to create a new directory.**

**touch - a command used to create an empty file or update the timestamp of an existing file.**

**cp (copy) - a command used to copy files or directories from one location to another.**

**cp -r (recursive copy) - a command used to copy directories and their contents recursively.**

**mv (move) - a command used to move files or directories and also to rename them.**

**rm (remove) - a command used to delete files.**

**rm -r (recursive remove) - a command used to delete directories and their contents recursively.**

**rmdir - a command used to delete an empty directory.**

**Globbing - a shell mechanism used to match filename patterns using wildcard characters.**

**Wildcard - special characters such as `*`, `?`, and `[]` used to match patterns in filenames.**

**Pipe (|) - a shell operator that passes the output of one command as input to another command.**

**Redirection (>, >>) - a shell feature used to redirect command output into a file, where “>” overwrites and “>>” appends to the file.**

## Відповіді на теоретичні питання

### Порівняння файлових структур Windows-подібної та Linux-подібної системи
Файлова структура операційних систем Windows і Linux побудована за ієрархічним принципом, але між ними є певні відмінності.

У Windows використовується система дисків, які позначаються літерами (C:, D:, E: тощо). Кожен диск має власну окрему структуру папок. Для користувача верхнім рівнем зазвичай є «This PC» або «My Computer», де відображаються всі доступні диски.

У Linux застосовується інший підхід - тут існує одна єдина файлова система, яка починається з кореневого каталогу, що позначається символом «/». Усі файли та каталоги знаходяться всередині цієї структури. У Linux немає букв дисків, оскільки фізичні пристрої підключаються (монтуються) до певних каталогів у межах загальної системи.

Також у Linux структура каталогів є стандартизованою (наприклад, /home, /etc, /usr), що робить систему більш впорядкованою. У Windows розташування системних файлів може відрізнятися залежно від версії операційної системи.

Отже, основна різниця полягає в тому, що Windows працює з окремими дисками, позначеними літерами, а Linux має єдину файлову структуру з коренем «/».

### Поняття FHS та його використання
FHS (Filesystem Hierarchy Standard) - це стандарт, який визначає структуру каталогів у Linux-подібних операційних системах.

Метою FHS є уніфікація розташування системних файлів і каталогів, щоб структура файлової системи була однаковою або максимально схожою у різних дистрибутивах Linux.

Наприклад:

/home - домашні каталоги користувачів

/etc - конфігураційні файли системи

/bin - основні виконувані файли

/usr - програми та бібліотеки користувача

/var - змінні дані (логи, кеш тощо)

Завдяки стандарту FHS адміністратори та користувачі можуть легко орієнтуватися в будь-якій Linux-системі, незалежно від дистрибутива.

### Основні команди для роботи з файлами та каталогами в Linux
Основними командами для роботи з файлами та каталогами в Linux є такі:

Для створення використовується команда touch, яка дозволяє створити порожній файл, та команда mkdir, за допомогою якої створюється новий каталог.

Для копіювання застосовується команда cp, що копіює файли, а також cp -r, яка дозволяє копіювати каталоги разом з усім їх вмістом.

Для переміщення або перейменування файлів і каталогів використовується команда mv. Вона може як переносити об’єкти в інше місце, так і змінювати їх назву.

Для видалення файлів застосовується команда rm, а для видалення каталогів разом із вмістом - rm -r. Якщо каталог порожній, його можна видалити за допомогою команди rmdir.

Усі ці команди є базовими та необхідними для роботи з файловою системою в операційній системі Linux.

## Хід роботи

### 1.1 Запуск операційної системи

Для виконання лабораторної роботи було запущено віртуальну машину VM2_Linux_CLI у середовищі VirtualBox. Після запуску відбулося завантаження операційної системи Linux Ubuntu в режимі командного рядка (CLI)

### 1.2 Опрацювання команд Lab 7 – Navigating the Filesystem

| Назва команди | Її призначення та функціональність |
|---------------|------------------------------------|
| pwd | Визначає місце знаходження користувача у файловій системі, показує поточну робочу директорію (print working directory). |
| echo $HOME | Виводить шлях до домашнього каталогу користувача через змінну середовища HOME. |
| cd / | Команда cd здійснює перехід до кореневого каталогу файлової системи. |
| cd | Команда cd без аргументів повертає користувача до домашнього каталогу. |
| cd /home | Здійснює перехід до каталогу /home за допомогою абсолютного шляху. |
| cd ~ | Здійснює перехід до домашнього каталогу користувача за допомогою символу ~. |
| cd ~root | Намагається перейти до домашнього каталогу користувача root (може виникнути помилка доступу). |
| cd /usr/bin | Здійснює перехід до каталогу /usr/bin за допомогою абсолютного шляху. |
| cd /usr | Здійснює перехід до каталогу /usr за допомогою абсолютного шляху. |
| cd /usr/share/doc | Здійснює перехід до каталогу /usr/share/doc. |
| cd bash | Здійснює перехід до підкаталогу bash за допомогою відносного шляху. |
| cd .. | Здійснює перехід на один рівень вище у файловій структурі. |
| cd ../dict | Переходить на один рівень вище та далі у каталог dict. |
| ls | Виводить список файлів та каталогів у поточній директорії. |
| ls -a | Виводить усі файли, включаючи приховані (які починаються з крапки). |
| ls -l | Виводить детальну інформацію про файл або каталог (тип, права доступу, власник, розмір, дата зміни). |
| ls -R | Виводить вміст каталогу разом із вмістом усіх його підкаталогів (рекурсивно). |
| ls -d | Відображає лише сам каталог без виведення його вмісту (часто використовується з шаблонами). |
| ls -d /etc/s* | Виводить файли та каталоги, назви яких починаються з літери «s». |
| ls -d /etc/???? | Виводить файли, назви яких складаються рівно з чотирьох символів. |
| ls -d /etc/[abcd]* | Виводить файли, назви яких починаються з літер a, b, c або d. |
| echo ~ ~sysadmin ~root ~mail ~nobody | Виводить шляхи до домашніх каталогів користувачів шляхом розгортання символу ~. |

### 1.3 Опрацювання команд Lab 8 – Managing Files and Directories

| Назва команди | Її призначення та функціональність |
|---------------|------------------------------------|
| echo * | Виводить усі файли та каталоги у поточній директорії (символ * відповідає нулю або більше символів). |
| echo D* | Виводить файли, назви яких починаються з літери D. |
| echo P* | Виводить файли, назви яких починаються з літери P. |
| echo *s | Виводить файли, назви яких закінчуються на літеру s. |
| echo D*n*s | Виводить файли, назви яких починаються з D та містять у середині символи n і s. |
| echo ?????? | Виводить файли, назви яких складаються рівно з шести символів (кожен ? відповідає одному символу). |
| echo D???????? | Виводить файли, назви яких починаються з D та складаються рівно з дев’яти символів. |
| echo ?????*s | Виводить файли, назви яких містять щонайменше шість символів і закінчуються на s. |
| echo [DP]* | Виводить файли, назви яких починаються з D або P (набір символів у квадратних дужках). |
| echo [!DP]* | Виводить файли, назви яких не починаються з D або P (заперечення набору через !). |
| echo [D-P]* | Виводить файли, назви яких починаються з літер у діапазоні від D до P. |
| echo [!D-P]* | Виводить файли, назви яких не починаються з літер у діапазоні від D до P. |
| cp /etc/hosts hosts | Копіює файл /etc/hosts у поточну директорію з новою назвою hosts. |
| cp -v /etc/hosts hosts | Копіює файл з відображенням інформації про процес копіювання (verbose-режим). |
| cp -v /etc/hosts . | Копіює файл у поточну директорію (символ «.» означає поточний каталог). |
| cp -p hosts /home/sysadmin | Копіює файл зі збереженням атрибутів (прав доступу та дати зміни). |
| cp -p /etc/hosts ~ | Копіює файл у домашній каталог користувача зі збереженням атрибутів. |
| cp hosts newname | Створює копію файлу з новою назвою newname. |
| cp -R /etc/udev Myetc | Копіює каталог разом із його вмістом (рекурсивне копіювання). |
| mkdir Myetc | Створює новий каталог Myetc. |
| rm hosts | Видаляє файл hosts. |
| rm hosts newname | Видаляє декілька файлів одночасно. |
| rm -r Myetc | Видаляє каталог разом із його вмістом рекурсивно. |
| rmdir directory | Видаляє порожній каталог. |
| touch premove | Створює порожній файл premove. |
| mv premove postmove | Переміщує або перейменовує файл premove у postmove. |
| rm postmove | Видаляє файл postmove. |

## 2. Робота в терміналі 
### 2.1 Визначення поточного робочого каталогу

Для визначення поточної робочої директорії було використано команду:
```bash
pwd
```
Команда pwd (print working directory) відображає повний шлях до каталогу, в якому наразі знаходиться користувач.

<img width="337" height="102" alt="image" src="https://github.com/user-attachments/assets/79788283-57a0-48ea-9d1d-92b074f42497" />

Рисунок 1 - Визначення поточного робочого каталогу.

### 2.2 Перехід до кореневого каталогу

Було виконано дві команди:
```bash
cd /
pwd
```
Команда cd / здійснює перехід до кореневого каталогу файлової системи.
Команда pwd підтверджує, що поточна директорія змінилася на /.

<img width="377" height="115" alt="image" src="https://github.com/user-attachments/assets/0d64e271-2de8-4156-810d-8e9aebbc90ba" />

Рисунок 2 - Перехід до кореневого каталогу.

### 2.3 Перегляд вмісту каталогу у довгому форматі

Для перегляду детальної інформації про файли використано:
```bash
ls -l
```
Ключ -l дозволяє відобразити:
- права доступу,
- власника,
- групу,
- розмір файлу,
- дату останньої зміни.

<img width="785" height="502" alt="image" src="https://github.com/user-attachments/assets/38ab49b9-5155-40fa-bb87-e3c0cca99b50" />

Рисунок 3 - Перегляд каталогу у довгому форматі.

### 2.4 Перехід до каталогу /usr/share

Було виконано:
```bash
cd /usr/share
pwd
```
Команда cd здійснює перехід до системного каталогу, а pwd підтверджує поточний шлях.

<img width="435" height="108" alt="image" src="https://github.com/user-attachments/assets/92304dc9-c27d-4810-b02e-d4d414ca7bcf" />

Рисунок 4 - Перехід до каталогу /usr/share.

### 2.5 Перегляд прихованих файлів

Для перегляду вмісту каталогу разом із прихованими файлами використано:
```bash
ls -a
```
Ключ -a відображає всі файли, включаючи приховані (їх назви починаються з символу .).

<img width="641" height="570" alt="image" src="https://github.com/user-attachments/assets/7df20bcc-4444-48ae-959f-93399c14350f" />

Рисунок 5 - Перегляд прихованих файлів.

### 2.6 Перехід до каталогу /etc
```bash
cd /etc
pwd
```
Каталог /etc містить системні конфігураційні файли.

<img width="480" height="118" alt="image" src="https://github.com/user-attachments/assets/dfb97cef-5f3d-4d4b-8a76-3d92357a1fb4" />

Рисунок 6 - Перехід до каталогу /etc.

### 2.7 Вибірка файлів за першою літерою імені

Оскільки ім’я - Yaroslava, використано:
```bash
ls y*
```
Символ * означає “будь-яка кількість символів після вказаної літери”.

<img width="572" height="87" alt="image" src="https://github.com/user-attachments/assets/0fe7de12-f19f-4492-bcd4-50dc3bdc2e7c" />

Рисунок 7 - Вибірка файлів за першою літерою.

### 2.8 Вибірка файлів із шести символів
```bash
ls ??????
```
Кожен символ ? відповідає рівно одному символу в назві файлу.

<img width="767" height="559" alt="image" src="https://github.com/user-attachments/assets/215e185f-6dd1-4a89-8f89-30c044cad485" />

Рисунок 8 - Вибірка файлів довжиною 6 символів.

### 2.9 Вибірка файлів за останньою літерою імен

Для імен Yaroslava, Diana, Dasha:
```bash
ls *[a]
```
Квадратні дужки [ ] дозволяють задати набір символів.

<img width="417" height="245" alt="image" src="https://github.com/user-attachments/assets/307d4058-38cc-4286-97cf-5602e4aaa713" />

Рисунок 9 - Вибірка файлів за останньою літерою.

2.10 Рекурсивний перегляд домашнього каталогу (через конвеєр)
```bash
cd ~
ls -R | sort -r
```
Команда | (конвеєр) передає результат однієї команди іншій.
sort -r виконує сортування у зворотному алфавітному порядку.

<img width="695" height="562" alt="image" src="https://github.com/user-attachments/assets/7e0d59e8-4e46-4e73-a5e4-6e654bf8ee2c" />

Рисунок 10 - Рекурсивний перегляд через конвеєр.

### 2.11 Створення каталогу групи
```bash
mkdir BIKS-33
```
Команда mkdir створює новий каталог.

<img width="625" height="149" alt="image" src="https://github.com/user-attachments/assets/042f9940-e9a1-4a38-9e8d-74a597e5f8ee" />

Рисунок 11 - Створення каталогу групи.

### 2.12 Перегляд оновленого вмісту домашнього каталогу
```bash
ls -r
```
Ключ -r відображає список у зворотному алфавітному порядку.

<img width="682" height="100" alt="image" src="https://github.com/user-attachments/assets/bd642307-cb55-4faf-b8f5-7dbbb6784732" />

Рисунок 12 - Оновлений вміст домашнього каталогу.

### 2.13 Створення файлу lab5
```bash
cd BIKS-33
touch lab5
ls
```
Команда touch створює порожній файл.

<img width="480" height="129" alt="image" src="https://github.com/user-attachments/assets/e7f4b544-522e-4b11-acec-649f8d53aab3" />

Рисунок 13 - Створення файлу lab5.

### 2.14 Створення підкаталогів
```bash
mkdir surname1 surname2 surname3
ls
```
Команда mkdir підтримує мультиаргументи — можна створити кілька каталогів одночасно.

<img width="734" height="116" alt="image" src="https://github.com/user-attachments/assets/ea1e9beb-4a38-491c-8b93-23e63bc1eb96" />

Рисунок 14 - Створення підкаталогів.

### 2.15 Створення файлу першого студента
```bash
cd surname1
touch name1
ls
```
cd surname1 - перехід у підкаталог першого студента

touch name1 - створення порожнього файлу name1

ls - перевірка, що файл справді з’явився

<img width="639" height="113" alt="image" src="https://github.com/user-attachments/assets/c67b67fb-30a8-4e28-a4f2-7aa4909f71f3" />

Рисунок 15 - Перехід у surname1 та створення файлу name1.

### 2.16 Запис інформації у файл name1
```bash
echo "Hello, my name is Name1" > name1
```
Команда echo виводить текст у термінал.
Символ > виконує перенаправлення виводу - текст записується безпосередньо у файл name1.
Якщо файл вже існує, його вміст буде перезаписано.

<img width="1229" height="78" alt="image" src="https://github.com/user-attachments/assets/1a92d62b-fb1a-429d-9de8-77207e5e3bad" />

Рисунок 16 - Запис даних у файл name1.

### 2.17 Перегляд вмісту файлу name1
```bash
cat name1
```
Команда cat відображає вміст файлу у терміналі.
Було підтверджено, що файл містить введену інформацію.

<img width="986" height="115" alt="image" src="https://github.com/user-attachments/assets/32c4b324-22ca-469a-a21a-7fc20ade497e" />

Рисунок 17 - Перегляд вмісту файлу name1.

### 2.18 Копіювання файлу та перевірка результату
```bash
cp name1 name2
ls
```
Команда cp name1 name2 створює копію файлу name1 з новим ім’ям name2 у поточному каталозі.

Після цього було використано команду ls, щоб перевірити результат виконання операції. У списку файлів відображаються обидва файли — name1 та name2, що підтверджує успішне копіювання.

<img width="660" height="106" alt="image" src="https://github.com/user-attachments/assets/4de4edb0-b2a2-4e65-84ba-74d49c178adb" />

Рисунок 18 - Копіювання файлу name1 та перевірка його наявності.

2.19 Перегляд вмісту файлу name2
```bash
cat name2
```
Було перевірено, що файл name2 містить повну копію вмісту файлу name1.

<img width="632" height="86" alt="image" src="https://github.com/user-attachments/assets/ac90d68a-efe8-4d42-8187-2b19e579e9c6" />

Рисунок 19 - Перевірка вмісту файлу name2.

2.20 Редагування файлу name2 та перевірка змін
```bash
echo "Hello, my name is Name2" > name2
cat name2
```
Команда echo з оператором перенаправлення > перезаписує вміст файлу name2 новим текстом.
Символ > означає повне перезаписування файлу.

Після цього за допомогою команди cat name2 було перевірено вміст файлу.
У результаті підтверджено, що файл містить оновлену інформацію.

<img width="660" height="106" alt="image" src="https://github.com/user-attachments/assets/98a17f2f-5d28-41e2-94d0-0e2be7b86ff7" />

Рисунок 20 - Оновлення та перевірка вмісту файлу name2.

### 2.21 Переміщення файлу name2 та перевірка результату
```bash
mv name2 ../surname2
ls
cd ../surname2
ls
```
Команда mv name2 ../surname2 переміщує файл name2 до каталогу surname2.
Символ .. означає перехід на один рівень вище в ієрархії каталогів.

Після виконання команди:

ls - перевірено, що у поточному каталозі файл name2 більше не відображається;

cd ../surname2 - виконано перехід до каталогу surname2;

ls - підтверджено, що файл name2 успішно переміщено.

<img width="710" height="165" alt="image" src="https://github.com/user-attachments/assets/e4007b28-8662-4743-b3ae-067fc6488cdf" />

Рисунок 21 - Переміщення файлу name2 до каталогу surname2 та перевірка результату.

### 2.22 Створення файлу третього студента
```bash
cp ../surname1/name1 name3
ls
```
Було створено копію файлу name1 з новим ім’ям name3.

<img width="760" height="93" alt="image" src="https://github.com/user-attachments/assets/57515170-80fe-4800-ac7b-7ff78d1f6261" />

Рисунок 22 - Створення файлу name3.

### 2.23 Переміщення файлу name3 та перевірка результату
```bash
mv name3 ../surname3
ls
cd ../surname3
ls
```
Команда mv name3 ../surname3 переміщує файл name3 до каталогу surname3.
Символ .. означає перехід на один рівень вище від поточного каталогу.

Після цього:
- команда ls підтверджує, що файл name3 більше не знаходиться у поточному каталозі;
- команда cd ../surname3 здійснює перехід до каталогу surname3;
- повторне виконання ls підтверджує, що файл name3 успішно переміщено.

<img width="725" height="163" alt="image" src="https://github.com/user-attachments/assets/30329cf5-f8ec-43cf-b3ea-62b7e7d75662" />

Рисунок 23 - Переміщення файлу name3 до каталогу surname3 та перевірка результату.

### 2.24 Перевірка вмісту файлу name3
```bash
cat name3
```
Було перевірено, що файл містить попередній текст (копію з name1).

<img width="589" height="91" alt="image" src="https://github.com/user-attachments/assets/5712c846-02ea-4e16-b0d7-24e08bef1a0a" />

Рисунок 24 - Перевірка початкового вмісту name3.

### 2.25 Редагування файлу name3
```bash
echo "Hello, my name is Name3" > name3
cat name3
```
Вміст файлу було оновлено та перевірено.

<img width="816" height="107" alt="image" src="https://github.com/user-attachments/assets/60376d37-852e-42af-a976-a40ed9e383b5" />

Рисунок 25 - Оновлений вміст файлу name3.

### 2.26 Повернення до домашнього каталогу користувача
```bash
cd ~
pwd
```
Команда cd ~ здійснює перехід до домашнього каталогу поточного користувача.
Команда pwd підтверджує, що поточний шлях змінився на:
```bash
/home/yaroslava
```

<img width="540" height="119" alt="image" src="https://github.com/user-attachments/assets/32b16ab9-5d14-4f8c-8e44-1b704f8d9e7f" />

Рисунок 26 - Повернення до домашнього каталогу користувача.

### 2.27 Рекурсивний перегляд каталогу групи
```bash
ls -R BIKS-33*
```
Команда ls -R виконує рекурсивний перегляд каталогу.
Ключ -R дозволяє відобразити всі підкаталоги та файли всередині.

Було використано glob-шаблон BIKS-33*, який дозволяє вибрати потрібний каталог.

У результаті відображено структуру каталогу:
- lab5
- Yaroslava
- Diana
- Dasha

та відповідні файли всередині кожного підкаталогу.

<img width="486" height="302" alt="image" src="https://github.com/user-attachments/assets/9f5efbaf-d00e-466c-8293-32564edf863e" />

Рисунок 27 - Рекурсивний перегляд структури каталогу групи.

### 2.28 Перейменування підкаталогів
```bash
mv surname1 Yaroslava
mv surname2 Diana
mv surname3 Dasha
ls
```
Команда mv використовується для перейменування або переміщення файлів і каталогів.
Було виконано перейменування підкаталогів відповідно до імен студентів.

Команда ls підтвердила успішне перейменування.

<img width="420" height="75" alt="image" src="https://github.com/user-attachments/assets/0391d785-1d13-49f3-869d-0a4db0dc13ca" />

Рисунок 28 - Перейменування підкаталогів.

### 2.29 Перейменування файлів усередині каталогів
```bash
mv name1 Yaroslava
mv name2 Diana
mv name3 Dasha
```
Було виконано перейменування файлів відповідно до імен студентів.

<img width="563" height="117" alt="image" src="https://github.com/user-attachments/assets/4a0ba383-cb78-4080-9d30-b41e08e3165c" />

Рисунок 29 - Перейменування файлів студентів.

### 2.30 Остаточна перевірка структури каталогу
```bash
ls -R BIKS-33
```
Було виконано фінальну перевірку структури каталогу групи.

У результаті отримано ієрархію каталогів з відповідними іменами студентів та файлами.

<img width="518" height="327" alt="image" src="https://github.com/user-attachments/assets/93f1f3e7-8a18-4254-849e-0edd7dfdad28" />

Рисунок 30 - Остаточна структура каталогу групи.

## 3. Опис команд для переміщення по системі каталогів

У процесі виконання лабораторної роботи було використано команду cd для навігації файловою системою Linux. Нижче наведено опис дій кожної з команд.

### 3.1 Команда cd /

Команда:
```bash
cd /
```
Виконує перехід до кореневого каталогу файлової системи.
Символ / означає найвищий рівень ієрархії, від якого починається структура всіх каталогів у Linux.

Після виконання команди поточним каталогом стає /.

### 3.2 Команда cd /home

Команда:
```bash
cd /home
```
Здійснює перехід до каталогу /home, у якому зберігаються домашні каталоги користувачів системи.

Це приклад використання абсолютного шляху, оскільки шлях починається з символу /.

### 3.3 Команда cd ~

Команда:
```bash
cd ~
```
Здійснює перехід до домашнього каталогу поточного користувача.

Символ ~ автоматично розгортається у шлях типу:
```bash
/home/ім'я_користувача
```
У даній роботі це: /home/yaroslava.

### 3.4 Команда cd (без аргумента)

Команда:
```bash
cd
```
Також виконує перехід до домашнього каталогу користувача.
Її дія повністю аналогічна команді cd ~.

### 3.5 Команда cd ..

Команда:
```bash
cd ..
```
Здійснює перехід на один рівень вище від поточного каталогу.

Символ .. означає батьківський каталог.

### 3.6 Команда cd ../..

Команда:
```bash
cd ../..
```
Дозволяє перейти на два рівні вище по ієрархії каталогів.
Кожен елемент .. означає підняття на один рівень.

### 3.7 Команда cd -

Команда:
```bash
cd -
```
Повертає користувача до попереднього каталогу, у якому він знаходився до останнього переходу.

Ця команда є зручною для швидкого перемикання між двома каталогами.

## Контрольні запитання

### 1. Як переглянути шлях до домашньої директорії користувача за допомогою echo (2 способи)
```bash
echo $HOME
echo ~
```
Команда echo $HOME виводить значення змінної середовища HOME, яка містить повний шлях до домашнього каталогу користувача.

Команда echo ~ використовує спеціальний символ ~, який автоматично розгортається у шлях до домашньої директорії.

<img width="434" height="137" alt="image" src="https://github.com/user-attachments/assets/46faf1b2-f6e4-4883-bcc2-b08c52cba69d" />

Рисунок 31 - Перегляд шляху до домашнього каталогу двома способами.

### 2. Чи можна переглянути вміст кореневого каталогу, перебуваючи у домашньому?

Так, можна. Переходити в кореневий каталог не потрібно.
```bash
pwd
ls /
```
Команда pwd підтверджує, що користувач знаходиться у домашньому каталозі.

Команда ls / виводить вміст кореневого каталогу незалежно від поточного розташування.

<img width="708" height="234" alt="image" src="https://github.com/user-attachments/assets/bd85e677-2ae4-4ab8-a07e-b78376cec1e4" />

Рисунок 32 - Перегляд вмісту кореневого каталогу без переходу до нього.

### 3. Як додати інформацію в порожній файл?
```bash
touch test.txt
echo "Hello" > test.txt
echo "World" >> test.txt
cat test.txt
```
- touch створює файл
- > записує текст у файл (перезаписує вміст)
- >> додає текст у кінець файлу
- cat показує вміст файлу

<img width="554" height="157" alt="image" src="https://github.com/user-attachments/assets/ad80d087-e6c9-400a-9166-069b239a539f" />

Рисунок 33 - Запис та додавання інформації у файл.

### 4. Як скопіювати та видалити існуючий каталог? Чи є різниця для непорожнього каталогу?
```bash
mkdir testdir
rmdir testdir
```
rmdir видаляє лише порожній каталог.
```bash
mkdir testdir
touch testdir/file.txt
rm -r testdir
```
rm -r видаляє каталог разом із його вмістом.

Різниця:
- для порожнього каталогу можна використовувати rmdir
- для непорожнього потрібен ключ -r

<img width="783" height="199" alt="image" src="https://github.com/user-attachments/assets/24d18280-f548-4224-8fd1-aa2df095da77" />

Рисунок 34 - Видалення порожнього та непорожнього каталогу.

### 5. У яких випадках відбувається переміщення, перейменування або обидві дії?

Команда:
```bash
mv /work/tech/comp.png /Desktop
```
→ лише переміщення

Команда:
```bash
mv /work/tech/comp.png /work/tech/my_car.png
```
→ лише перейменування

Команда:
```bash
mv /work/tech/comp.png /Desktop/computer.png
```
→ переміщення + перейменування одночасно

<img width="645" height="173" alt="image" src="https://github.com/user-attachments/assets/1c0f5ee9-349c-4510-8c1d-3b58542c9c07" />

Рисунок 35 - Демонстрація роботи команди mv.

## Conclusion 

During this laboratory work, the basic Linux commands for file system navigation and file/directory management were studied and practiced. I learned how to determine the current working directory, navigate between directories, and view their contents in different formats, including the use of glob patterns for file selection.

Practical skills were developed in creating, copying, moving, renaming, and deleting files and directories, including recursive deletion of non-empty directories. Special attention was given to output redirection using > and >>, as well as working with environment variables.

This laboratory work helped me better understand the structure of the Linux file system and the functionality of the command-line interface. The acquired skills are fundamental and essential for further work with Linux-based operating systems.
